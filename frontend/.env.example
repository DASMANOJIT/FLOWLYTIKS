# Next.js server-only env (used in next.config.ts rewrites)
# Production must point to the deployed backend service, not localhost.
BACKEND_URL=https://your-backend-service.onrender.com

# Optional debug flags (keep off in production)
NEXT_PUBLIC_RENDER_DEBUG=0
NEXT_PUBLIC_DEBUG_SIGNUP=0
